put 99-noto-overrides.conf $HOME/.var/app/com.brave.Browser/config/fontconfig/conf.d

It replaces all ugly rendering fonts with noto. I prfere seeing noto over these ugly fonts. Settings will only be applied to brave.
